// Placeholder. Owner: Person 5 — small badge for a thread's bucket
// (urgent / this_week / fyi) and/or numeric priority score.

export default function PriorityBadge() {
  return <span>PriorityBadge placeholder</span>;
}
