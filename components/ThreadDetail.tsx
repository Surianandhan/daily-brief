// Placeholder. Owner: Person 4 — renders a single thread's detail view
// (summary, tasks/deadlines, reasoning trace, reply draft).

export default function ThreadDetail() {
  return <div>ThreadDetail placeholder</div>;
}
