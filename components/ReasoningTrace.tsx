// Placeholder. Owner: Person 5 — shows the score breakdown
// (senderImportance / deadlineProximity / actionRequired) for a thread.

export default function ReasoningTrace() {
  return <div>ReasoningTrace placeholder</div>;
}
